# node-sqlite-form

Simple Node.js + SQLite form example.

## Setup
1. Install dependencies:

```powershell
npm install
```

2. Run the server:

```powershell
npm start
```

3. Open http://localhost:3000 and submit the form.

4. Inspect the `database.db` file with `sqlite3` or a DB browser.
