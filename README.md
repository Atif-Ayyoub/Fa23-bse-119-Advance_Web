# my_mongo_app

Simple Node.js + Express + MongoDB example.

## Setup

1. Copy `.env.example` to `.env` and update `MONGODB_URI`.
2. Run:

```
npm install
npm start
```

Open `http://localhost:5000` to use the HTML form or call the JSON API at `/api/users`.
